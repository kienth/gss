import CustomBody from "../../components/CustomBody";
import CustomTable from "../../components/CustomTable";

const Janitorial = () => {
  return (
    <CustomBody isLoading={false} header="Janitorial">
      <CustomTable row={[]} column={[]} isLoading />
    </CustomBody>
  );
};

export default Janitorial;
